<?php

if ( ! defined( 'ABSPATH' ) ) {
    exit;
}

add_action( 'widgets_init', 'bastovan_register_sidebars' );

function bastovan_register_sidebars() {
    // Za sada tema ne koristi widgete
    // Dodati ako zatreba
}